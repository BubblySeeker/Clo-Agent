# Landing Page Plan

## Layout

Keep the current layout structure — text on the left, visual on the right.

## Hero Section (Above the Fold)

- **Left side**: Keep existing copy/CTA layout as-is
- **Right side**: Replace current image with a **3D interactive dashboard mockup**
  - Floating/hovering effect (slight elevation shadow, perspective transform)
  - User can **spin/rotate** the dashboard (mouse drag or auto-rotate on idle)
  - Shows a realistic preview of the actual CloAgent dashboard
  - Subtle parallax or tilt on mouse hover

## Below the Fold — 6 Key Features

Scroll down to reveal 6 feature cards showcasing the core AI CRM capabilities.

**Format**: Same card layout style as the current Pipeline / Contact Manager / Security cards — icon, title, short description, visual preview.

### The 6 Features:

1. **AI Assistant** — Natural language queries, tool-calling, confirmations
2. **Smart Pipeline** — Kanban board with drag-drop deal management
3. **Contact Intelligence** — Buyer profiles, AI-generated summaries, activity timeline
4. **Task Management** — Due dates, priorities, smart follow-up reminders
5. **Analytics & Reports** — Pipeline charts, activity breakdown, source tracking
6. **Activity Tracking** — Calls, emails, notes, showings logged per contact

### Card Style:
- Match existing card design (rounded corners, subtle shadow, icon + title + description)
- Each card could include a small screenshot or animated preview of that feature
- Grid layout: 2 columns x 3 rows or 3 columns x 2 rows

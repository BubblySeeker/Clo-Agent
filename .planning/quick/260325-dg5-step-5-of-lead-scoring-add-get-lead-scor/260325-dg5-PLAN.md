---
phase: quick
plan: 260325-dg5
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-service/app/tools.py
  - ai-service/tests/test_tools.py
  - ai-service/app/services/agent.py
autonomous: true
requirements: [LEAD-SCORE-STEP-5]

must_haves:
  truths:
    - "get_lead_score tool exists in TOOL_DEFINITIONS with contact_id required param"
    - "get_lead_score is classified as a read tool"
    - "execute_read_tool routes get_lead_score to its executor function"
    - "Executor queries lead_score, lead_score_signals, previous_lead_score and returns structured result with score, tier, dimensions, top signals, and suggested action"
    - "Morning Briefing prompt instructs AI to call get_lead_score and include lead score data"
    - "All 3 new tests pass in test_tools.py"
  artifacts:
    - path: "ai-service/app/tools.py"
      provides: "get_lead_score tool definition, READ_TOOLS entry, executor routing, _get_lead_score function"
    - path: "ai-service/tests/test_tools.py"
      provides: "3 new test methods for get_lead_score"
    - path: "ai-service/app/services/agent.py"
      provides: "Updated Morning Briefing prompt with lead score injection"
  key_links:
    - from: "ai-service/app/tools.py"
      to: "TOOL_DEFINITIONS list"
      via: "get_lead_score dict entry"
    - from: "ai-service/app/tools.py"
      to: "execute_read_tool function"
      via: "elif branch routing to _get_lead_score"
---

<objective>
Add the get_lead_score read tool to the AI service (tool #35: 19 read + 16 write). This gives the AI the ability to retrieve and present a contact's lead score with tier, dimension breakdown, top signals, and suggested action. Also update the Brief Me system prompt to include lead score data in morning briefings.

Purpose: Enable the AI to answer questions like "what's John's lead score?" and surface lead scores in daily briefings.
Output: Updated tools.py with new tool + executor, updated test_tools.py with 3 tests, updated agent.py with briefing prompt changes.
</objective>

<execution_context>
@/Users/matthewfaust/CloAgent/Clo-Agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewfaust/CloAgent/Clo-Agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@ai-service/app/tools.py
@ai-service/app/services/agent.py
@ai-service/tests/test_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_lead_score tool definition, READ_TOOLS entry, executor routing, and private executor function</name>
  <files>ai-service/app/tools.py</files>
  <action>
1. Add a new tool definition dict to TOOL_DEFINITIONS list (after the send_email entry, before the closing `]` at line 523):

```python
{
    "name": "get_lead_score",
    "description": "Get a contact's lead score with tier classification, dimension breakdown, top signals, and suggested next action. Use this when the user asks about a contact's score, priority, or engagement level.",
    "input_schema": {
        "type": "object",
        "properties": {
            "contact_id": {"type": "string", "description": "UUID of the contact to get lead score for"},
        },
        "required": ["contact_id"],
    },
},
```

2. Add `"get_lead_score"` to the READ_TOOLS set (line ~546, after "draft_email").

3. Add an elif branch in execute_read_tool (before the `else` at line ~619):
```python
elif tool_name == "get_lead_score":
    return await run_query(lambda: _get_lead_score(agent_id, tool_input["contact_id"]))
```

4. Add private executor function `_get_lead_score(agent_id: str, contact_id: str) -> dict` following the existing pattern (uses `get_conn()`, `psycopg2.extras.RealDictCursor`). Place it after _get_contact_details (around line 718, before _get_contact_activities).

The function should:
- Query: `SELECT lead_score, lead_score_signals, previous_lead_score FROM contacts WHERE id = %s AND agent_id = %s`
- If contact not found, return `{"error": "Contact not found"}`
- If lead_score is None, return `{"error": "No lead score calculated yet for this contact"}`
- Compute tier from score: 80+ = "Hot", 50-79 = "Warm", 25-49 = "Cool", below 25 = "Cold"
- Parse lead_score_signals (JSONB, already a dict/list when loaded by psycopg2):
  - Extract dimension scores from the signals structure. The signals JSONB has a `dimensions` key with `{engagement, readiness, velocity, profile}` sub-objects each having a `score` field.
  - Extract `top_signals` — the top 3 items from the `signals` list in the JSONB, each having `label` and `points` fields.
- Compute suggested_action based on tier:
  - Hot: "High priority — reach out today, this lead is actively engaged"
  - Warm: "Good momentum — schedule a follow-up within 2-3 days"
  - Cool: "Needs nurturing — send relevant listings or market updates"
  - Cold: "Re-engage — consider a check-in call or remove from active pipeline"
- Compute trend from previous_lead_score: "rising" if current > previous, "falling" if current < previous, "stable" if equal, "new" if previous is None
- Return dict with keys: `score`, `tier`, `trend`, `previous_score`, `dimensions` (dict of dimension name -> score), `top_signals` (list of top 3 signal dicts with label + points), `suggested_action`
  </action>
  <verify>
    <automated>cd /Users/matthewfaust/CloAgent/Clo-Agent && python -c "
import sys; from unittest.mock import MagicMock
sys.modules.setdefault('app.database', MagicMock())
from app.tools import TOOL_DEFINITIONS, READ_TOOLS
names = [t['name'] for t in TOOL_DEFINITIONS]
assert 'get_lead_score' in names, 'Tool not in TOOL_DEFINITIONS'
assert 'get_lead_score' in READ_TOOLS, 'Tool not in READ_TOOLS'
tool = next(t for t in TOOL_DEFINITIONS if t['name'] == 'get_lead_score')
assert 'contact_id' in tool['input_schema']['required'], 'contact_id not required'
print('PASS: tool definition, READ_TOOLS entry, and schema verified')
"</automated>
  </verify>
  <done>get_lead_score appears in TOOL_DEFINITIONS with contact_id required, is in READ_TOOLS, has elif routing in execute_read_tool, and _get_lead_score executor function exists with correct query and tier/trend/signal logic</done>
</task>

<task type="auto">
  <name>Task 2: Add 3 tests for get_lead_score to test_tools.py</name>
  <files>ai-service/tests/test_tools.py</files>
  <action>
Add 3 new test methods to the TestToolDefinitions class (after test_create_property_schema, before the TestToolTriggerMapping class at line 164):

1. `test_get_lead_score_exists_in_definitions` — asserts that a tool with name "get_lead_score" exists in `self.definitions` (use `any(t["name"] == "get_lead_score" for t in self.definitions)`)

2. `test_get_lead_score_is_read_tool` — asserts `"get_lead_score" in self.read_tools`

3. `test_get_lead_score_requires_contact_id` — finds the get_lead_score tool definition, asserts `"contact_id"` is in `schema["properties"]` and `"contact_id"` is in `schema["required"]`

Follow the exact pattern used by test_create_contact_schema and test_create_task_schema (use `next(t for t in self.definitions if t["name"] == ...)` for schema extraction).
  </action>
  <verify>
    <automated>cd /Users/matthewfaust/CloAgent/Clo-Agent && python -m pytest ai-service/tests/test_tools.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 3 new tests pass, all existing tests still pass, zero failures in test_tools.py</done>
</task>

<task type="auto">
  <name>Task 3: Update Brief Me system prompt to include lead score data</name>
  <files>ai-service/app/services/agent.py</files>
  <action>
In _build_system_prompt (agent.py), update the MORNING BRIEFING section (lines 252-274). After the existing step 4 (`list_deals`), add a new step 5 to the tool-calling chain:

After line 260 (the `"  4. list_deals — scan for stale deals..."` line), add:
```python
"  5. For each contact with a deal, call get_lead_score to get their lead score. "
"Include lead scores in the briefing — flag Hot leads for immediate action and "
"Cold leads that may need re-engagement or pipeline cleanup.\n"
```

Then in the briefing format section, after the "### Action Items" description (around line 266-268), add a new section instruction:
```python
"  ### Lead Score Alerts — contacts with Hot scores (80+) that need immediate action, "
"and contacts whose scores are falling (trending down from previous). Skip this section "
"if no contacts have lead scores yet.\n"
```

This is prompt engineering only — no new function calls in Python code. The AI model will use the existing get_lead_score tool based on these instructions.
  </action>
  <verify>
    <automated>cd /Users/matthewfaust/CloAgent/Clo-Agent && python -c "
import sys; from unittest.mock import MagicMock
sys.modules.setdefault('app.database', MagicMock())
from app.services.agent import _build_system_prompt
prompt = _build_system_prompt('Test Agent')
assert 'get_lead_score' in prompt, 'get_lead_score not in system prompt'
assert 'Lead Score' in prompt, 'Lead Score section not in briefing format'
print('PASS: Brief Me prompt updated with lead score instructions')
"</automated>
  </verify>
  <done>Morning Briefing system prompt includes instruction to call get_lead_score for contacts with deals and a Lead Score Alerts section in the briefing format</done>
</task>

</tasks>

<verification>
All tasks verified by automated checks:
1. Tool definition exists with correct schema
2. All test_tools.py tests pass (existing + 3 new)
3. System prompt contains lead score instructions
</verification>

<success_criteria>
- get_lead_score is tool #35 (19 read + 16 write) in TOOL_DEFINITIONS
- get_lead_score is classified as a read tool
- Executor function queries lead_score, lead_score_signals, previous_lead_score from contacts table
- Returns structured response with score, tier, trend, dimensions, top_signals, suggested_action
- 3 new tests pass in test_tools.py
- Brief Me prompt instructs AI to call get_lead_score and display Lead Score Alerts section
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/quick/260325-dg5-step-5-of-lead-scoring-add-get-lead-scor/260325-dg5-SUMMARY.md`
</output>
